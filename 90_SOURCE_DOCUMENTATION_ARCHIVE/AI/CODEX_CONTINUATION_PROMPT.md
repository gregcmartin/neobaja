# Full Codex continuation prompt

Continue `/Users/gregmartin/Desktop/BAJA Outrun` directly with Codex. Do not
launch Grok or UltraGrok. The archived Grok workflow is `interrupted`; existing
Grok-generated raw art and provenance remain valid local historical inputs.

Read all files in `/Users/gregmartin/Desktop/BAJA Outrun/AI` beginning with
`README.md`, `CURRENT_STATE.md`, `CODEX_HANDOFF.md`, `GAMEPLAY_ORACLE.md`, and
`VERIFICATION.md`, then inspect the current project and
`docs/GAUNTLET_SCORECARD.md`. Confirm no old writer is active and make a
rollback copy because the target has no Git repository.

Objective: finish the native Neo Geo BAJA Outrun game to the user's four pinned
Neo Geo mockups as the primary presentation bar and the eight pinned official
BAJA: Edge of Control HD screenshots as a secondary physicality bar. Use actual
image-capable inspection of every reference and every cited fresh 320x224 MAME
capture. Filenames, metadata, hashes, and prose do not count as looking.

The persisted current verdict is FAIL at 17/28. First repair the black racing
line void caused by `punch_road_funnel()` exceeding actual projected-road
coverage. Use the existing hashed raw stage/rut art and deterministic
conversion before considering new art. After each single-gap repair, rebuild,
capture fresh nonzero-speed MAME frames separated by unique gameplay VBlanks,
and use a fresh independent critic. The seven categories are composition/road,
vehicle identity/scale, stage density/identity, motion/physicality, native
pixel craft, HUD/minimap/type, and full-flow cohesion. Overall VISUAL PASS
requires every category >=3 and total >=24/28.

Preserve the exact unskippable developer splash on gameplay VBlanks 0–299 and
title at 300; Max/Cruz selection; blue 2022 Polaris RZR Pro R; distinct real
RZR/Maverick challengers; steering, throttle, braking, collision, projected
course motion, finish/next-stage, and all four stages. Use the pinned
ZgzInfinity/OutRun commit only as a clean-room behavior oracle. Copy no GPL
source/constants/data/assets/maps/presentation. GAMEPLAY PASS additionally
requires a real off-road penalty and sequential telemetry proving handling,
challenger decisions/overtake/order, collision/non-collision, finish, and
next-stage. Overall PASS requires both VISUAL and GAMEPLAY PASS.

Existing builds and provenance must work offline. Do not call Grok preflight.
If new bitmap generation is truly required, use Codex's imagegen skill/tool and
record its exact real provider, prompt, raw path/hash, deterministic conversion,
source hash, and visual review; never relabel it as Grok. Keep reference pixels
nonshipping. Keep BIOS external. Work locally only and report physical hardware
as UNVERIFIED unless it is actually tested.
