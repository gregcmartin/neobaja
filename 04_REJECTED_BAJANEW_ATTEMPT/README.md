# Rejected BAJANEW implementation — documentation quarantine

The 2026-08-30 BAJANEW game implementation was deleted from the working tree
after Greg's live verdict that its implementation and playability were
unacceptable. The art was approved and remains under `../art/`.

This folder contains documentation only. It is a failure record, not a source
of engine design, gameplay tuning, constants, architecture, tests, tools,
build configuration, ROM layout, telemetry, screenshots, or benchmarks for a
future implementation.

For disaster recovery only, the exact rejected state is locally recoverable at
Git tag `rejected-bajanew-playability-v1` (commit `c24d1fe`). Future agents must
not inspect or reuse material from that tag unless Greg explicitly widens the
clean-room boundary.
